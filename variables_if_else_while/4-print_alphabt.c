#include<stdio.h>

/**
 * main - print alphabet
 * Return: 0
 */

int main(void)
{
        char alp;

        for (alp = 'a'; alp <= 'z'; alp++); alp != 'q'; alp != 'e')
        {
        putchar(alp);
        }
        putchar('\n');
        return (0);

}
