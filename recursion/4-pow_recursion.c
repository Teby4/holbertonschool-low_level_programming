#include "main.h"

/**
 * _pow_recursion - recursion
 * Return:
 * Description:
 * @x: number
 * @y: number
 */

int _pow_recursion(int x, int y)
{
	if (y < 0)
	{
		return(-1);
	}
	if (x <= 1)
	{
		return (_pow_recursion(n * y));
	}
}
