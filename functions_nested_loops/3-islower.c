#include<stdio.h>
#include"main.h"

/**
 * _islower - check if the letter is lowercase
 * Return: 
 */

int _islower(int c)
{
	int il;

	if (islower(il) != 0)
	{
	return (1);
	}
	else
	{
	return (0);
	}

}
