#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *
 *
 *
 *
 *
 */

char *create_array(unsigned int size, char c)
{
	char **ar;

	c = malloc(size);
	if (c == '\0')
	{
	return ('\0');
	}
	while (size < c)
	{
	return(ar[size]);
	}
	if (size == 0)
	{
	return (0);
	}
	free(size);
}
