#include "main.h"
void print_chessboard(char (*a)[8])
/**
 * print_chessboard - ME QUIERO MORIR !!!!
 * Return:
 * Description:
 * @a: array
 */

void print_chessboard(char (*a)[8])
{
	int cont, cont2;

	for (cont = 0; cont < 8; cont++;)
	{
		for (cont2 = 0; con2 < 8; cont ++)
		{
		_putchar(a[cont][cont2])
		}
	_putchar('\n')
	}
}
