#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks for digit
 * Return: 1 or 0
 * Description:
 * @c: number input
 */

int _isdigit(int c)
{
	for (c >= 0; c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
